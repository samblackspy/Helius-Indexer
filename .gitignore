# --- General / OS ---
.DS_Store
Thumbs.db
*.log
*.log*
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- IDE / Editor Config ---
.vscode/
!.vscode/settings.json # Optional: Allow shared settings if desired
!.vscode/extensions.json # Optional: Allow shared extensions if desired
.idea/
*.iml
*.sublime-project
*.sublime-workspace

# --- Environment Variables (IMPORTANT!) ---
# Ignore all .env files by default
.env
.env.*
# Except for example files (if you create them)
!*.env.example
# Specifically ignore local overrides for frontend
.env.local
.env.*.local

# --- Node.js Dependencies ---
node_modules/

# --- Build Output ---
# Worker build output (from tsconfig.json)
helius-indexer-worker/dist/
# Frontend build output (Vite default)
helius-supabase-indexer-ui/dist/
# Other potential build folders
build/
out/
.output/
.next/ # If you ever switch to Next.js

# --- Supabase ---
# Ignore Supabase CLI generated state and secrets
supabase/.supabase/
# Ignore temporary files potentially generated by Supabase CLI locally
supabase/temp/

# --- Test / Coverage Reports ---
coverage/
*.lcov

# --- TypeScript ---
*.tsbuildinfo

# --- Optional: Add specific files if needed ---
# e.g., my-secret-config.json